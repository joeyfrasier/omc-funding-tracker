# Omnicom Pay Run Funding Solution - Credentials Template
# Copy to .env and fill in values

# === Aggregate Database ===
DB_HOST=aggregate.XXXXX.eu-west-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=production
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# SSH Tunnel (required for DB access)
SSH_BASTION_HOST=xx.xx.xx.xx
SSH_BASTION_PORT=22
SSH_KEY_PATH=/path/to/.ssh/db-bastion.pem

# === MoneyCorp API ===
MONEYCORP_API_URL=https://corpapi.moneycorp.com
MONEYCORP_LOGIN_ID=your_login_id
MONEYCORP_API_KEY=your_api_key

# === Gmail Service Account ===
SERVICE_ACCOUNT_FILE=service-account.json
GMAIL_IMPERSONATE=your_email@worksuite.com

# === CORS (comma-separated origins for production) ===
# CORS_ORIGINS=https://your-app.example.com

# === SSH Tunnel ===
# Set to true if using an external SSH tunnel (e.g. autossh)
# SSH_TUNNEL_DISABLED=false
